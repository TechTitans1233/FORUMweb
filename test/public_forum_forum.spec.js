/**
 * Teste gerado automaticamente para `public/forum/forum.js`
 */
const mod = require('../../public/forum/forum.js');

describe('forum.js', () => {
  test('deve exportar algo definido', () => {
    expect(mod).toBeDefined();
  });
});
