/**
 * Teste gerado automaticamente para `public/login/login.js`
 */
const mod = require('../../public/login/login.js');

describe('login.js', () => {
  test('deve exportar algo definido', () => {
    expect(mod).toBeDefined();
  });
});
