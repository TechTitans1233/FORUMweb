/**
 * Teste gerado automaticamente para `server.js`
 */
const mod = require('../../server.js');

describe('server.js', () => {
  test('deve exportar algo definido', () => {
    expect(mod).toBeDefined();
  });
});
