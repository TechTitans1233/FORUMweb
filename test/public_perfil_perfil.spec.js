/**
 * Teste gerado automaticamente para `public/perfil/perfil.js`
 */
const mod = require('../../public/perfil/perfil.js');

describe('perfil.js', () => {
  test('deve exportar algo definido', () => {
    expect(mod).toBeDefined();
  });
});
