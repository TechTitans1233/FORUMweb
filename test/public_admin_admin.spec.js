/**
 * Teste gerado automaticamente para `public/admin/admin.js`
 */
const mod = require('../../public/admin/admin.js');

describe('admin.js', () => {
  test('deve exportar algo definido', () => {
    expect(mod).toBeDefined();
  });
});
